import { EgFocusDirective } from './eg-focus.directive';

describe('EgFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new EgFocusDirective();
    expect(directive).toBeTruthy();
  });
});
