import { UncleanHtmlPipe } from './unclean-html.pipe';

describe('UncleanHtmlPipe', () => {
  it('create an instance', () => {
    //const pipe = new UncleanHtmlPipe();
    //expect(pipe).toBeTruthy();
  });
});
